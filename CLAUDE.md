# CLAUDE.md

## 이 프로젝트

개인 프로필 / 링크 페이지 (link in bio). 인스타그램, 블로그 등의 링크를
한 페이지에 모으는 정적 사이트. **모바일 화면을 우선**으로 만든다.

배포는 GitHub Pages, 도메인은 `elkiss.me` (HTTPS 적용 완료).
블로그(`blog.elkiss.me`)는 별도 저장소(`blog`)에 있다.

## 작업 규칙

### push는 사용자가 정한다

원칙은 그대로다 — **push 시점은 사용자가 정한다.** Claude가 알아서 올리지 않는다.

- 작업이 명확하면 → 커밋 메시지까지 작성해서 **커밋까지만** 한다
- 변경이 복잡하거나 판단이 필요하면 → **staged 상태까지만** 두고 멈춘다

**로컬 작업 (사용자 PC)**

Claude는 `git push` 를 실행하지 않는다. 사용자가 직접 한다.
**브랜치를 따지 않고 `main` 에 바로 커밋한다.** 어차피 사용자가 직접 올리므로
PR 절차는 형식만 늘린다. (2026-08-11 지정)

**Anthropic 클라우드 환경 (Claude Code on the web 등)**

여기서는 작업이 격리된 컨테이너 안에서 일어나고, **사용자가 그 안의 저장소에
직접 접근할 수 없다.** push 를 금지하면 작업물이 컨테이너와 함께 사라진다.
그래서 이 환경에서만 예외를 둔다.

- 커밋까지 해두고 **push 명령을 기다린다.** 시키지 않으면 올리지 않는다
- 사용자가 push 하라고 하면 그때 Claude 가 직접 실행한다
- **올리기 전에 무엇이 공개되는지 먼저 알린다.** public 저장소라 되돌리기 어렵다
- 지정된 작업 브랜치에만 올린다. `main` 에 직접 올리지 않는다

### 커밋 메시지는 한글로, 짧게 (2026-08-12 지정)

**제목 한 줄 + 본문 3~5줄.** 왜 그렇게 정했는지는 `docs/LOG.md` 에 적는다.
커밋 메시지에 또 적으면 **같은 얘기가 두 군데 있게 되고, 두 군데 있는 것은 어긋난다.**

- 문서가 전부 한글이다. 커밋만 영어일 이유가 없다
- `git log --oneline` 으로 훑어지는 길이를 지킨다
- 자세한 근거가 필요하면 본문에 `docs/LOG.md` 날짜를 가리킨다

이 규칙 이전 커밋은 영어에 스무 줄이 넘는다. **고치지 않는다** —
이미 있는 히스토리를 다시 쓸 값어치가 없다.

### 커밋은 잘게 쪼개지 않는다

시안을 주고받으며 다듬는 동안에는 파일만 고쳐두고 커밋하지 않는다.
**한 덩어리가 끝났을 때 한 번에** 커밋한다.

서로 상쇄되는 커밋(넣었다 뺀 것, 세 번 고친 문구)이 히스토리에 남을 값어치는 없다.
왜 그렇게 정했는지는 어차피 문서에 적는다.

### 커밋 준비 시 민감 정보를 점검한다

**public 저장소다.** 한 번 push되면 되돌리기 어렵다.
커밋을 준비할 때마다 아래를 확인하고, 걸리는 게 있으면 커밋 전에 알린다.

- 이메일 주소, 비밀번호, API 키, 토큰, 인증서, 개인키
- 실명, 주소, 전화번호
- 로컬 절대 경로 (`D:\...`, `/Users/...`)
- 커밋 작성자 이메일 (`user.email`) — 파일이 아니라 커밋 메타데이터에
  들어가고, public 저장소에서는 그대로 공개된다

### 커밋 준비 시 `박다` 표현을 점검한다

커밋을 묶을 때 문서에 `박다` 계열 표현(`변수를 박다`, `박아 넣다`,
`박혀 있다` 등)이 있는지 확인하고, 있으면 다른 표현으로 바꾼다.

- `변수를 박다` → `변수를 하드코딩한다`
- `문장 속에 박혀 있다` → `문장 속에 섞여 있다`

눈으로만 보지 말고 **한 번 훑는다.** 커밋을 준비할 때와 push 직전에 확인한다.

```
git ls-files -z | xargs -0 grep -nI '박' | grep -v '^CLAUDE.md:'
```

**아무것도 안 나와야 통과다.** `CLAUDE.md` 는 규칙을 적으려면 금지어를 예시로
쓸 수밖에 없어서 뺀다. 이걸 안 빼면 매번 걸려서 점검이 무뎌진다.
`반박`·`소박` 처럼 관계없는 낱말도 같이 걸리니 눈으로 한 번 거른다.

### 그 외

- 한 번에 한 단계씩. 시키지 않은 범위까지 미리 만들지 않는다
- 판단이 필요한 결정은 근거를 붙여 문서에 남긴다. 나중에 다시 정하지 않도록