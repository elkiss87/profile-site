# CLAUDE.md

## 이 프로젝트

개인 프로필 / 링크 페이지 (link in bio). 인스타그램, 블로그 등의 링크를
한 페이지에 모으는 정적 사이트. **모바일 화면을 우선**으로 만든다.

배포는 GitHub Pages 예정, 나중에 커스텀 도메인을 붙인다.

## 작업 규칙

### push는 사용자만 한다

Claude는 **절대 `git push`를 실행하지 않는다.** 예외 없음.

- 작업이 명확하면 → 커밋 메시지까지 작성해서 **커밋까지만** 한다
- 변경이 복잡하거나 판단이 필요하면 → **staged 상태까지만** 두고 멈춘다
- push는 항상 사용자가 직접 한다

### 커밋 준비 시 민감 정보를 점검한다

**public 저장소다.** 한 번 push되면 되돌리기 어렵다.
커밋을 준비할 때마다 아래를 확인하고, 걸리는 게 있으면 커밋 전에 알린다.

- 이메일 주소, 비밀번호, API 키, 토큰, 인증서, 개인키
- 실명, 주소, 전화번호
- 로컬 절대 경로 (`D:\...`, `/Users/...`)
- 커밋 작성자 이메일 (`user.email`) — 파일이 아니라 커밋 메타데이터에
  들어가고, public 저장소에서는 그대로 공개된다

### 그 외

- 한 번에 한 단계씩. 시키지 않은 범위까지 미리 만들지 않는다